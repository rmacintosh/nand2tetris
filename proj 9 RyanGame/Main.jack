/**
 * The Main class initializes a new Marble Solitaire game and starts it.
 */
class Main {

    /** Initializes a new game and starts it. */    
    function void main() {
        var MarbleSolGame game;

        let game = MarbleSolGame.new();
        do game.run();
        do game.dispose();

        return;
    }
}
